## Packages
framer-motion | For smooth page transitions and micro-interactions
date-fns | For formatting dates nicely

## Notes
- Using Google Fonts: 'Playfair Display' for headings, 'DM Sans' for UI.
- Auth is handled via `useAuth` hook from `@/hooks/use-auth`.
- Dashboard layout includes a sidebar and top navigation.
